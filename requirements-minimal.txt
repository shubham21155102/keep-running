# Minimal requirements - Fast installation (without heavy ML models)
# Use this for GitHub Actions for faster setup

# Core
requests>=2.31.0
python-dotenv>=1.0.0

# PDF Processing
pdfplumber>=0.10.0

# Langchain & Vector Store (Lightweight versions)
langchain>=0.1.0
langchain-core>=0.1.0
langchain-community>=0.1.0
langchain-text-splitters>=0.1.0

# Snowflake
snowflake-connector-python>=3.3.0

# Pydantic
pydantic>=2.0.0

# Embeddings & Re-ranking (Pre-built binaries)
sentence-transformers>=2.2.0
numpy>=1.24.0
scipy>=1.10.0

# FAISS (CPU only - faster to install)
faiss-cpu>=1.7.4

# Note: torch and huggingface embeddings are optional
# They're installed as dependencies of sentence-transformers
